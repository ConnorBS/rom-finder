[pytest]
# Put the repo root on sys.path so `import app` works regardless of how pytest
# is invoked (CI runs `pytest tests/`; `python -m pytest` happened to add cwd).
pythonpath = .
testpaths = tests
