# ROM Finder configuration
# Copy this file to .env and fill in your values

# Server
HOST=127.0.0.1
PORT=8080
DEBUG=false

# Database (SQLite by default, stored next to the app)
DB_URL=sqlite:///./rom_finder.db
